# Copy to .env (gitignored) and fill in, or export these directly in your shell/MCP client config.
ASC_KEY_ID=
ASC_ISSUER_ID=
# Absolute path to the .p8 private key file. Never commit the key itself.
ASC_KEY_PATH=
